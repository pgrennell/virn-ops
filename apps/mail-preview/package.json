{
	"name": "mail-preview",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "email dev --port 3003 --dir ../../packages/mail/emails",
		"clean": "git clean -xdf .cache .turbo dist node_modules",
		"lint": "oxlint .",
		"format": "oxfmt ."
	},
	"dependencies": {
		"react": "catalog:",
		"react-email": "^6.1.4"
	},
	"devDependencies": {
		"@repo/tsconfig": "workspace:*",
		"@types/node": "^25.7.0",
		"@types/react": "^19.2.14"
	}
}
